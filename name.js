function spellName(name) {
    for (let i = 0; i < name.length; i++) {
        console.log(name[i]);
    }
}

let name = "Samuel Nathaniel";
spellName(name.toLowerCase()); // or name.toUpperCase() for uppercase
